<%= form_with(model:[book,post_comment],url: book_post_comments_path(@book)) do |f| %>
					<p></p>
					<div class="row">
						<div class=" col-xs-12">
					 		<%= f.text_area :comment, rows:"5", class: "form-control", placeholder: "コメントをここに" %>
					 	</div>
					</div>
					<p></p>
					<%= f.submit"送信する", class: "btn btn-basic btn-primary col-xs-12"%>
					<% end %>